import React from "react";
import ReactDOM from "react-dom/client";
import RiceMillSystem from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RiceMillSystem />
  </React.StrictMode>
);
